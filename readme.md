To run tabular models, just run "paper_model_fitting.py". To run time series models, run bryce_models.py. All data will be automatically downloaded.

Run data visualizations by running bryce_visualizations.py

Models are saved to cache/.
Elo Data is saved to data/.
Model performance plots are saved to plots/

Dependencies managed with UV.